const Discord = require('discord.js');
const db = require('quick.db');

module.exports = {
  config: {
    name: "blacklist",
    aliases: ["bl", "bluser", "blacklist"]

  
},
run: async(bot, msg, args) => {
  
  
  
}}